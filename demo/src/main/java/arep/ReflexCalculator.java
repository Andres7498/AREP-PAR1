package arep;
import java.lang.Math;

public class ReflexCalculator {
    public static double calculator(String metodo, double resolve){
        Math.getMethod(metodo, resolve);
        return resolve;
    }
//**Ejemplo de algoritmo:
//QUICKSORT (arreglo A, inicio, fin)     
//{  
//  if (inicio < fin)     
//  {  
//     p = partition(A, inicio, fin)  
//     QUICKSORT (A, inicio, p - 1)    
//     QUICKSORT (A, p + 1, fin)    
// }  
//}   
}
